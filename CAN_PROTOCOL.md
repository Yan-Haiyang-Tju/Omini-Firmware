# OMINI_FOC CAN 通信协议及例程

## 一、硬件连接

```
PC (USB-CAN 上位机)
    │ USB
    ▼
USB-CAN 适配器 (WitMotion, AT指令模式)
    │ CAN_H / CAN_L  (加上 120Ω 终端电阻)
    ▼
TJA1050 CAN 收发器 → STM32F103 (PA11=CAN_RX, PA12=CAN_TX)
```

## 二、USB-CAN 适配器配置

| 参数 | 值 |
|------|------|
| 工作模式 | **AT 指令模式** |
| CAN 波特率 | **1 Mbps** |
| 帧类型 | 标准数据帧 (11-bit ID) |
| 上位机操作 | 帧 ID 栏填十六进制，数据栏填十六进制 |

## 三、设备地址

| 参数 | 值 |
|------|:---:|
| 设备 ID | **20** (0x14) |
| 修改方式 | `User_CAN.h` → `CAN_DEVICE_ID` |

## 四、CAN ID 计算公式

```
控制帧 ID = (设备ID << 5) + 9
          = (20 << 5) + 9
          = 0x0289

状态帧 ID = (设备ID << 5) + 17
          = (20 << 5) + 17
          = 0x02A9
```

---

## 五、命令帧格式 (ID = 0x0289, DLC = 8)

### 字节布局

| Data[0] | Data[1] | Data[2] | Data[3] | Data[4] | Data[5-7] |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 0x00 | 0x14 | mode | val_hi | val_lo | 0x00 |

### 控制模式

| mode | 命令 | val 含义 |
|:---:|------|------|
| 0x00 | 复位/重新校准 | 忽略 (填 0) |
| 0x01 | 力矩控制 | val ÷ 100 = 力矩 |
| 0x02 | 位置控制 | val ÷ 100 = 目标角度 (rad) |

> **角度单位**: 弧度 (rad)。0rad = 夹爪闭合位置。正值 = 张开。

---

## 六、位置控制例程

### 张开 10 rad

```
帧 ID: 0289
数据:   00 14 02 03 E8 00 00 00
        ─┬─ ─┬─ ─┬─ ──┬── ──┬──
        ID  ID mode val  保留
        H   L  02  1000÷100=10rad
```

### 常用位置命令速查

| 目标 | val (十进制) | val (十六进制) | 数据 (HEX) |
|------|:---:|------|------|
| 0 rad (闭合) | 0 | 0x0000 | `00 14 02 00 00 00 00 00` |
| 5 rad | 500 | 0x01F4 | `00 14 02 01 F4 00 00 00` |
| 10 rad | 1000 | 0x03E8 | `00 14 02 03 E8 00 00 00` |
| 15 rad | 1500 | 0x05DC | `00 14 02 05 DC 00 00 00` |
| 20 rad | 2000 | 0x07D0 | `00 14 02 07 D0 00 00 00` |
| 30 rad | 3000 | 0x0BB8 | `00 14 02 0B B8 00 00 00` |
| 50 rad | 5000 | 0x1388 | `00 14 02 13 88 00 00 00` |
| 70 rad | 7000 | 0x1B58 | `00 14 02 1B 58 00 00 00` |
| 90 rad (全开) | 9000 | 0x2328 | `00 14 02 23 28 00 00 00` |

---

## 七、力矩控制例程

### 力矩 0.40

```
帧 ID: 0289
数据:   00 14 01 00 28 00 00 00
              ─┬─ ──┬──
            mode  40÷100=0.40
```

### 力矩 0.20

```
帧 ID: 0289
数据:   00 14 01 00 14 00 00 00
              ─┬─ ──┬──
            mode  20÷100=0.20
```

---

## 八、参数设置 (使用 Data[1] 参数选择)

位置/速度/电流限制可在任何时候通过 CAN 修改，立即生效。

| Data[1] | 参数 | Data[3-4] 含义 |
|:---:|------|------|
| 0x01 | **速度限制 rad/s** | val ÷ 10 |
| 0x02 | **电流限制 (Iq 上限, 0~2)** | val ÷ 100 |
| 0x03 | **位置 Kp** | val ÷ 10 |
| 0x04 | **位置 Kd** | val ÷ 10 |
| 0x05 | **速度 Kp** | val ÷ 1000 |
| 0x06 | **速度 Ki** | val ÷ 10000 |
| 0x07 | **加速度限制 rad/s²** | val ÷ 10 |

### 设置速度限制 6 rad/s

```
帧 ID: 0289
数据:   00 01 00 00 3C 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=60 → 60÷10=6 rad/s
```

### 设置电流限制 0.5A

```
帧 ID: 0289
数据:   00 02 00 00 32 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=50 → 50÷100=0.5
```

### 设置位置 Kp = 1.5

```
帧 ID: 0289
数据:   00 03 00 00 0F 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=15 → 15÷10=1.5
```

### 设置位置 Kd = 7.0

```
帧 ID: 0289
数据:   00 04 00 00 46 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=70 → 70÷10=7.0
```

### 设置速度 Kp = 0.05

```
帧 ID: 0289
数据:   00 05 00 00 32 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=50 → 50÷1000=0.05
```

### 设置速度 Ki = 0.002

```
帧 ID: 0289
数据:   00 06 00 00 14 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=20 → 20÷10000=0.002
```

### 设置加速度限制 5 rad/s²

```
帧 ID: 0289
数据:   00 07 00 00 32 00 00 00
        ─┬─ ─┬─ ──┬──
       param val=50 → 50÷10=5 rad/s²
```

---

## 九、复位校准

```
帧 ID: 0289
数据:   00 14 00 00 00 00 00 00
              ─┬─ ──┬──
            mode val=0
```

电机重置：力矩推到底 → 释放 → 记录新零点。

---

## 十、状态反馈帧 (ID = 0x02A9)

STM32 每 100ms 自动发送一次，DLC = 8。

| Data[0-1] | Data[2-3] | Data[4-5] | Data[6-7] |
|:---:|:---:|:---:|:---:|
| 位置×100 | 速度×10 | Iq×1000 | Id×1000 |
| (int16 大端) | (int16 大端) | (int16 大端) | (int16 大端) |

### 解读示例

```
收到: 13 88 00 0A 00 64 FF 9C

位置 = 0x1388 = 5000 ÷ 100  = 50.0 rad
速度 = 0x000A = 10   ÷ 10   = 1.0 rad/s
Iq   = 0x0064 = 100  ÷ 1000 = 0.100 A
Id   = 0xFF9C = -100 ÷ 1000 = -0.100 A (有符号)
```

> **注意**: Id/Iq 为 有符号 int16，负值用补码表示 (如 -100 = 0xFF9C)。

---

## 十一、启动行为

```
上电
 ├─ AS5600 初始化
 ├─ CAN 初始化 (1Mbps)
 ├─ FOC 初始化 (PWM 10kHz)
 ├─ AlignRotor (转子零位)
 ├─ 夹爪闭合校准:
 │    ├─ 力矩模式 Q=0.08 推 3 秒
 │    ├─ 释放 0.5 秒
 │    └─ 记录零位 (gripper_zero)
 ├─ 切换到位置模式
 └─ 等待 CAN/串口命令
```

---

## 十二、串口命令（仍可用）

| 发送 | 效果 |
|------|------|
| `P 50` | 目标位置 = 50 rad |
| `P 0` | 回到闭合位置 (0 rad) |
| `P 10` | 张开 10 rad |

---

## 十三、Python 控制示例 (PC)

```python
import serial

ser = serial.Serial('COM22', 115200, timeout=1)

# 构建 CAN 帧 (AT 指令模式)
def make_can_frame(can_id, data):
    frame  = [0x41, 0x54]           # "AT"
    frame += [0x00]                  # 标准数据帧
    frame += [0x00, 0x00]           # ID 高 2 字节
    frame += [(can_id >> 8) & 0xFF, can_id & 0xFF]  # ID 低 2 字节
    frame += [8]                     # DLC = 8
    frame += data                    # 8 字节数据
    frame += [0x0D, 0x0A]           # \r\n
    return bytes(frame)

# 位置控制: 张开 50 rad
data = [0x00, 0x14, 0x02, 0x13, 0x88, 0x00, 0x00, 0x00]
ser.write(make_can_frame(0x0289, data))

# 力矩控制: Q=0.40
data = [0x00, 0x14, 0x01, 0x00, 0x28, 0x00, 0x00, 0x00]
ser.write(make_can_frame(0x0289, data))

# 复位校准
data = [0x00, 0x14, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]
ser.write(make_can_frame(0x0289, data))
```
